"""Per-channel baseline from the steady Phase-1 window.

TODO(Logan):
- fit(records) -> {channel: (mean, std)} over a clean baseline window.
- channels: execution_accuracy (overall + per difficulty), query_valid rate, complexity_gap, latency, tokens.
"""
